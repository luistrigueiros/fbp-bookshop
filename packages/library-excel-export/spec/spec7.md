Having implemented [](./spec6.md) now implement the following:

1. Verify that the export job status is allways updated in the table even in the event of one expected error.
2. Define and use one enum for the job status, rather than using strings.
Having implemented [](spec3.md) now implement the following:

1. Make use of https://bun.com/docs/guides/runtime/tsconfig-paths#re-map-import-paths to make the imports work.
2. Add a GitHub action to this subpackage to run the tests automatically on every push.
3. The GitHub action for this subpackage should be similar to the [data-layer-ci.yml](../../../.github/workflows/data-layer-ci.yml)
Having implemented all of the [](./consolidated-spec.md) fix the bug with the display of the [](../src/frontend/components/BookFilters.tsx) in mobile devices.
At the moment when the filter popup is displayed, the left-most size of the popup is not visible.
In the mobile devices, the labels for the filters are rendering with all the text visible, for example the label "Publisher" only "lisher" is visible on the screen 
and the same is happening for all the other labels and the input fields.
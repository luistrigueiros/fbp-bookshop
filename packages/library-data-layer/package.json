{
  "name": "library-data-layer",
  "version": "1.0.0",
  "description": "Data access layer for Library book shop management system using Drizzle ORM and Cloudflare D1",
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "drizzle",
    "README.md"
  ],
  "scripts": {
    "prepare": "bun run build && bun run build:types",
    "postinstall": "bun run build && bun run db:migrate:local",
    "build:types": "bunx tsc --project tsconfig.build.json --emitDeclarationOnly",
    "typecheck": "bunx tsc --noEmit",
    "build": "rm -rf dist && bun run build:types && bun build src/index.ts --outdir dist --target node",
    "dev": "bun run src/index.ts",
    "preconfig": "bun ../../scripts/config.ts",
    "db:generate": "bun run preconfig && bun drizzle-kit generate",
    "db:migrate:local": "bun run db:generate && bun wrangler d1 migrations apply library-db --local --remote=false",
    "db:setup:local": "bun run preconfig && ([ -d drizzle ] && bun wrangler d1 migrations apply library-db --local || echo 'No migrations to apply')",
    "db:migrate:prod": "bun run preconfig && bun drizzle-kit push",
    "db:studio": "bun run preconfig && bun drizzle-kit studio",
    "lint": "bunx eslint .",
    "test": "bun test",
    "test:coverage": "bun test --coverage || true",
    "format": "bunx prettier --write ."
  },
  "keywords": [
    "drizzle",
    "cloudflare",
    "d1",
    "library",
    "database"
  ],
  "dependencies": {
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/bun": "catalog:",
    "drizzle-kit": "catalog:",
    "miniflare": "catalog:",
    "tsx": "^4.21.0",
    "wrangler": "catalog:"
  },
  "peerDependencies": {
    "@cloudflare/workers-types": "^4.0.0"
  }
}
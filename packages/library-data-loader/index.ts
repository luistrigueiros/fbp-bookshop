import { name as dataLayerName } from "library-data-layer";
import { name as excelExtractorName } from "library-excel-extractor";

export const name = "library-data-loader";
console.log(`Loading data using ${dataLayerName} and ${excelExtractorName}`);
